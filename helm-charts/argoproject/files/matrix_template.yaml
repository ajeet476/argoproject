metadata:
  name: '{{.name}}-{{.namespace}}'
spec:
  project: default
  source:
    repoURL: GIT_REPO
    targetRevision: REPO_REVISION
    path: '{{.path}}'
  destination:
    server: K8S_SERVER
    namespace: '{{.namespace}}'
