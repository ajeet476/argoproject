name: cpn-api
# Where to deploy in Kubernetes
namespace: dev-cpn

# Where your Helm chart is stored
chartRepoURL: mydomain

# e.g., 'main', 'v1.0.0'
targetRevision: 1.0.0

# Path to your Helm chart within the repository
chartPath: helm-quarkus/
